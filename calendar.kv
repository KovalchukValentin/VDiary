#:import FadeTransition kivy.uix.screenmanager.FadeTransition

<CalendarApp>
    title: "Calendar"
    icon: self.icon

<MainWindow>
    name: "month"

<SideBar>
    anim_type: 'fade_in'
    #'slide_above_anim'
    #'reveal_below_simple'

<Main_layout>
    orientation: "vertical"

<Top_menu_layout>
    size_hint: (1, .2)

<Week_layout>
    size_hint: (1, .2)

<Bottom_layout>
    size_hint: (1, .2)

########################################################
<DayWindow>
    name: "day"

<Day_layout>
    orientation: "vertical"

<Day_topbar>
    size_hint: (1, .12)

<DayInfo>
    orientation: "vertical"

<Day_bottom_layout>
    size_hint: (1, .12)

#####################################################

<ChangeMonthWindow>
    name: "change_month"

<Change_month_layout>
    orientation: "vertical"

<ChangeYearWindow>
    name: "change_year"

